{
  "name": "emotion-mcp",
  "version": "0.1.0",
  "description": "MCP server for managing character expressions with transitions",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server/index.ts",
    "dev:http": "bun run src/server/http-server.ts",
    "build": "bun build src/server/index.ts --outdir dist",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "open": "^11.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.13"
  }
}
